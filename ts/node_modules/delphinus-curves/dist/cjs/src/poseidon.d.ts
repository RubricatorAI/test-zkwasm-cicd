import { Field } from './field.js';
export declare class Poseidon {
    private config;
    private state;
    private absortbing;
    private squeezed;
    constructor(config: any);
    getState(): Field[];
    private permute;
    update_exact(elements: Field[]): Field;
    update(elements: Field[]): void;
    squeeze(): Field;
}
export declare function poseidon(inputs: Field[]): Field;
